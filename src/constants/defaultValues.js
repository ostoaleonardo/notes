export const DEFAULT_CATEGORIES = [
    { id: 'all', name: 'All' }
]

export const DEFAULT_NOTE_CATEGORIES = ['all']

export const DEFAULT_LIST = { type: '', items: [] }

export const DEFAULT_ITEM_LIST = { value: '', status: 'unchecked' }

export const DEFAULT_SORT = {
    field: 'updated',
    order: 'desc'
}
